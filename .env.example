VITE_API_BASE_URL=http://localhost:8000

# Preferred: real Google sign-in.
VITE_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_ID=

# Optional local fallback. Use with `npm run dev:local` and
# `npm run dev:api:local` instead of Google sign-in.
VITE_ALLOW_LOCAL_AUTH=true
